//
// Prefix header for all source files of the 'ProVoc' target in the 'ProVoc' project
//

#ifdef __OBJC__
    #import <Cocoa/Cocoa.h>
#endif

#define DISABLE_QTKIT
#define DISABLE_SOUNDRECORDER
#define DISABLE_CHECKFORUPDATES // until the previous implementation has been replaced with Sparkle and the required infrastructure (certificates, appcast feed, etc.) is all set
#define DISABLE_SUBMITTER
